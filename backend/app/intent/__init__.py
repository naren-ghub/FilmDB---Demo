# FilmDB — Intent classification package
# Tier 1: DomainClassifier (embedding cosine similarity)
# Tier 2: Per-domain sub-intent agents (narrow LLM prompts)
# Tier 3: Entity extraction (runs once, outside classification)
# Entry point: HybridIntentClassifier.classify(message)

